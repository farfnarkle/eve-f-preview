# v1.0.11

## Added

- **Ignore double-clicks on cycle mouse buttons.** New option on the General tab for side buttons that register one press as two clicks and skip a client. When it is on, only that extra click (within 100 ms) is ignored, so you can still cycle as fast as you press. Off by default. Keyboard cycle hotkeys are unchanged.
